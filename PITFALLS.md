# ⚠️ 西西崽测评站 - 开发踩坑清单

> 每次开发新测评或改功能前，**必须读这个文件**。
> 这里的每一条都是血泪教训，犯了就是重复劳动。
> PRD.md 描述"做什么"，本文件描述"别再犯什么"。

---

## 一、结果页内容规范（最高优先级）

### ❌ 不要做的事
- 结果页只有2段描述+3条场景就完事 → 七七会说"太简略""敷衍"
- 充电方式/建议只写一句话 → 七七会看到空白区块
- 放「生成分享图」按钮 → 七七明确不要分享功能
- 描述用干巴巴的功能说明文体 → 没有情绪、没有代入感

### ✅ 必须做的事
结果页必须包含以下区块，每个区块都要有实际内容：
1. **耗电指数/天赋指数**（大字分数+进度条+等级文字）
2. **多维度评分条**（带颜色等级，红/黄/蓝，主类型高亮）
3. **核心描述**（至少3-4段，有场景代入、有洞察、有共情）
4. **金句引用框**（用户原话，带左竖线+斜体）
5. **Top3场景列表**（带序号圆+具体场景描述）
6. **适合的工作机制**（至少3条，带✓标记）
7. **职场雷区**（至少3条，带✗标记）
8. **充电方式**（至少3句，含具体可执行建议，不能只有一句话）
9. **特质标签**（5个圆角tag，帮助用户识别自己）
10. **次要类型提示**（告诉用户第二高的是什么）

---

## 二、产品详情文案规范

### ❌ 不要做的事
- 详情写成功能说明书（"15道题测出6种类型，每种附带XX"）→ 太干，没有购买欲
- 详情里出现具体URL链接 → 七七明确要求不放
- 详情里写具体价格数字 → 用"几十元"模糊化

### ✅ 必须做的事
参考NO.001 career的详情结构：
1. **场景代入开头**（"你是不是也有过这种感觉——"）
2. **痛点共鸣**（描述用户的真实困境，2-3句）
3. **认知翻转**（"不是你XX，是你YY"）
4. **产品价值**（这个测评能帮你解决什么）
5. **用户证言**（2-3条"做完的人都在说"）
6. **行动召唤**（X道题，Y分钟，测完你会XX）
7. **趣味收尾**（⚠️ 温馨提示：测完可能会想XX）

---

## 三、发货说明规范

### 好评福利写法
- 大概提一下赠送什么，不出现具体URL
- 当前文案：「五星好评+截图找客服，免费领取额外测评一份（测你的隐藏天赋型格，含详细职业路线+盲区自查）」
- 如有变更，在此处更新

---

## 四、Admin后台规范

### 每次新增产品必须检查
1. **products对象里有该产品的完整数据**（title/shortTitle/detail/link/linkUrl/commentGuide/svgs）
2. **product-chip切换按钮加上了**（漏加=七七看不到产品）
3. **admin.html和admin2.html同步更新**
4. **drain产品detail用双引号包裹**（单引号+换行会出问题）

---

## 五、JS语法踩坑

### 引号嵌套
- ASCII双引号字符串内**不能**再出现ASCII双引号 → SyntaxError
- 解决方案：内部引号用中文引号『』替代
- 单引号字符串内可以用ASCII双引号，但注意`\n`要转义

### 必须做的检查
- 每次写完JS：提取script内容 → `node --check` 验证语法
- 不要只检查语法，还要检查**运行时**是否有未定义的函数被调用

---

## 六、NO.003 天赋型格测评踩坑

1. AB二选一区分度不够 → 每题应该4选项，每选项给不同维度加分
2. 维度组合未映射 → 导致结果显示undefined
3. DOM元素缺失 → renderX函数被调用但未定义，JS直接中断
4. 只做语法检查不做运行检查 → 语法过了但页面空白
5. 附赠手册和测评结果内容重复 → 手册应提供测评里没有的深度内容
6. 结果页内容太粗糙 → 七七明确反馈"内容太粗糙""区分度不够"

---

## 七、缓存问题

- Cloudflare Pages有Edge缓存 + 手机浏览器缓存
- 每次推送后用 `?v=随机数` 访问
- 如果仍看到旧版：Cloudflare Dashboard → Caching → Purge Everything
- admin用admin2.html绕过缓存

---

## 八、数据一致性规则

1. **详情承诺的评分项，结果页必须都有**
2. **所有选项的type值，必须在results对象里有对应定义**
3. **dimNames必须覆盖所有结果类型**
4. **每个结果类型的字段完整性**：icon/title/badge/desc/quote/scenarios/fit/traps/charge/tags 全部不能少

---

## 九、设计原则

1. **移动优先**：max-width:500px
2. **点击即交互**：不需要多余确认步骤
3. **每个产品用不同配色**（NO.001绿/NO.002红/NO.003紫/NO.004深蓝）
4. **题目全部原创**
5. **产品矩阵互不干扰**：每个测评独立文件，admin统一管理

---

## 十、飞书采集表规则

- 采集表ID：`NzKEbIDkbav5cpsE70kcFKBrnjb` / `tblt7wXAJ6hSl3wL`
- **飞书API默认按创建时间排序（最早在前）**，与界面视图排序不一致
- **不能用「前N行」定位记录**，必须`page_size=500`全量读取后按字段内容筛选
- 判断需要写的新记录：有「标题」和「选择svg模版库类型」，但没有「新标题」/「正文」/「svg代码」的记录

---

## 更新日志
- 2026-08-04：创建此文件，从PRD.md和踩坑经历中提取所有规则

## 十一、小红书笔记标题规范

### 核心原则：标题只给疑问/半个结论，答案留给正文

用户刷小红书只在决定「要不要点开」上花0.5秒。标题把答案说完了 = 没有点开欲望。

### ❌ 不要做的事
- 标题把答案直接说完（"不是清高，是身体说不出口"→ 答案给了，不点）
- 标题用问句+自问自答（"为什么被说不够社会化？答案太扎心了"→ 油腻套路）
- 标题太长太完整，像一句话而不是一个钩子

### ✅ 正确做法
用采集表爆款标题做对标：

| 标题 | 为什么有效 |
|------|------------|
| 「感觉INFP不适合上班不是因为性格」 | 说到一半，好奇"那是因为什么" |
| 「为什么说INFP是职场恶人最讨厌的一类人」 | 制造冲突感（恶人vs INFP） |
| 「总算是知道INFP为什么工作不稳定了」 | 结论先行但不给原因 |
| 「INFP是不是天生缺了一个职场功能」 | 好奇"什么功能？" |
| 「INFP的精力到底去哪了」 | 好奇"去哪了？" |

### 标题公式
1. **半个结论型**：感觉/发现/终于知道INFP + X（不给原因）
2. **疑问型**：INFP是不是/到底 + X？（不给答案）
3. **冲突型**：INFP + 反直觉结论（制造想点进去反驳/确认的冲动）

### 正文写作规范
- 200-300字/篇
- 必须引用采集表评论区用户原话（不是凭空编）
- 短段落+\n换行
- 结尾引导测试+话题标签

